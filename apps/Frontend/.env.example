# Copia este archivo a .env.local y ajusta si es necesario.
# .env.local nunca se commitea (ya esta en .gitignore).

# URL base de la API del backend (lib/api/client.ts). Debe apuntar al
# puerto donde corre apps/Backend (PORT en su .env, default 3000).
NEXT_PUBLIC_API_URL="http://localhost:3000"
